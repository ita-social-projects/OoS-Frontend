import { ApplicationChildFilterPipe } from './application-child-filter.pipe';

describe('ApplicationChildFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationChildFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
