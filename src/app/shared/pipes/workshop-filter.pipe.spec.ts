import { WorkshopFilterPipe } from './workshop-filter.pipe';

describe('WorkshopFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkshopFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
