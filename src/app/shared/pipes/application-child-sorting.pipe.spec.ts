import { ApplicationChildSortingPipe } from './application-child-sorting.pipe';

describe('ApplicationChildSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationChildSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
