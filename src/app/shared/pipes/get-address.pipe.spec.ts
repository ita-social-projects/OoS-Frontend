import { GetAddressPipe } from './get-address.pipe';

describe('GetAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
