import { GetFullAddressPipe } from './get-full-address.pipe';

describe('GetFullAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFullAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
