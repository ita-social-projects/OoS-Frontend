import { ReasonTransformPipe } from './reason-transform.pipe';

describe('ReasonTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new ReasonTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
