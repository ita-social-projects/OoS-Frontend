import { DeclinationPipe } from './declination.pipe';

describe('DeclinationPipe', () => {
  it('create an instance', () => {
    const pipe = new DeclinationPipe();
    expect(pipe).toBeTruthy();
  });
});
