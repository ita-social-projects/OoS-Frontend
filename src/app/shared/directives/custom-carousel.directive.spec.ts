import { CustomCarouselDirective } from './custom-carousel.directive';

describe('CustomCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
