import { BoxtipDirective } from './boxtip.directive';

describe('BoxtipDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxtipDirective();
    expect(directive).toBeTruthy();
  });
});
