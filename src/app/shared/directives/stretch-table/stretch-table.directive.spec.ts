import { StretchTableDirective } from './stretch-table.directive';

describe('StretchTableDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchTableDirective();
    expect(directive).toBeTruthy();
  });
});
