export enum Role {
  provider = 'provider',
  parent = 'parent',
  unauthorized = 'unauthorized'
}

export enum cardType {
  provider = 'provider',
  parent = 'parent',
  child = 'child',
  user = 'user',
}

export enum RoleLinks {
  provider = 'організацію',
  parent = 'дитину'
}
